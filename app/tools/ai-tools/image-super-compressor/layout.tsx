import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Ultra Image Compressor | Shrink Files 90% without Quality Loss | Tulz.net",
  description: "Compress images by up to 90% using browser-based AI. Free, private, and no uploads required. Perfect for web optimization.",
  keywords: ["image compression", "compress image", "reduce file size", "optimize images", "browser compression", "free compressor"],
}

export default function ImageSuperCompressorLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

import { Metadata } from "next"

export const metadata: Metadata = {
  title: "AI Background Remover | Private & Free Browser Tool | Tulz.net",
  description: "Remove image backgrounds using AI that runs in your browser. Free, private, and powered by WebAssembly for fast processing.",
  keywords: ["ai background remover", "remove background ai", "browser background remover", "wasm background removal"],
}

export default function AIBackgroundRemoverProLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
